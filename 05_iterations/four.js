const myObj = {
    js : 'Javascript',
    cpp : 'C++',
    rb : 'ruby',
    swift : 'swift by apple'
}

for (const key in myObj) {
    // console.log(`${key} shortcut for a ${myObj[key]}`);
    }


const programming = ["js", "py", "c++", "java", "cpp", "c",]

for (const key in programming) {                                            // in for in loop it print only keys not a value // array also have a key
    // console.log(key);
    // console.log(programming[key]);                      // for a value
}


map.set('IN', 'India')
map.set('USA', 'United States of America')
map.set('AUS', 'Austalia')
map.set('GER', 'Germany')

for (const key in map) {
    // console.log(key);                           // there is a error/ // // there are other methods for use this
}


